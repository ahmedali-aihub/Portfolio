# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local settings
.claude/

# Local env files. VITE_* values are baked into the public bundle at build
# time, so never put a real secret in one — server secrets live in
# backend/.env, which is ignored separately.
.env
.env.local
.env.*.local
!.env.example
